<configuration>

    <!-- ===================== FILE APPENDER ===================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>Micdl-etl.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Rotazione giornaliera + dimensione max per file -->
            <fileNamePattern>Micdl-etl.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss} %class.%method - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== CONSOLE APPENDER ===================== -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{dd-MM-yyyy HH:mm:ss} 
                %highlight(%-5level){
                    INFO=green,
                    DEBUG=yellow,
                    ERROR=red
                } 
                %cyan(%class.%method) - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- ===================== LOGGER ROOT ===================== -->
    <root level="DEBUG">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
